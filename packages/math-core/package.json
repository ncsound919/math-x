{
  "name": "@mathx/math-core",
  "version": "0.4.0",
  "private": true,
  "description": "Shared math utilities, prompt schemas, and engine helpers for Math X",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
