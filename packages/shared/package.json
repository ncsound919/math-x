{
  "name": "@mathx/shared",
  "version": "0.5.0",
  "private": true,
  "description": "Shared types, constants, and utilities for Math X",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
